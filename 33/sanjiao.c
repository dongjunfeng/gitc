#include <stdio.h>

int main(void)
{
    int i, j, t, q;

    for(i = 50; i <= 100; i++)
    {
        for(j = 1; j <= i; j++)
        {
            
        }
        printf("%d = %d * %d *%d\t", i, j, t, q);
    }

    return 0; 
}
